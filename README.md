# High Win Rate - Frontend

فرانت‌اند فروشگاه استراتژی‌های معاملاتی با Next.js

## 🚀 راه‌اندازی سریع

### روش سریع (پیشنهادی) ⭐

```bash
./start-dev.sh
```

این اسکریپت به صورت خودکار:
- بررسی وجود Node.js و npm
- ایجاد فایل `.env` از `.env.example` (در صورت نیاز)
- نصب dependencies
- راه‌اندازی سرور توسعه

### روش دستی

1. **نصب dependencies**
```bash
npm install
```

2. **تنظیم متغیرهای محیطی**
```bash
cp .env.example .env
# ویرایش فایل .env در صورت نیاز
```

3. **راه‌اندازی سرور توسعه**
```bash
npm run dev
```

فرانت‌اند در آدرس `http://localhost:3001` در دسترس خواهد بود.

## 📋 قابلیت‌ها

### صفحات اصلی

- **صفحه اصلی** (`/`) - نمایش محصولات برتر
- **لیست محصولات** (`/products`) - نمایش همه محصولات
- **جزئیات محصول** (`/products/[id]`) - نمایش جزئیات و خرید
- **ورود** (`/login`) - ورود به حساب کاربری
- **ثبت‌نام** (`/register`) - ایجاد حساب کاربری جدید
- **داشبورد** (`/dashboard`) - مدیریت محصولات، دوره‌ها و فایل‌های خریداری شده

### ویژگی‌ها

- ✅ احراز هویت کامل (ثبت‌نام/ورود)
- ✅ نمایش محصولات با نرخ برد
- ✅ سیستم کد تخفیف
- ✅ خرید با ارز دیجیتال (Mock Gateway)
- ✅ نمایش دوره‌ها و فایل‌های خریداری شده
- ✅ دانلود فایل‌ها
- ✅ UI/UX مدرن با Tailwind CSS

## 🛠️ تکنولوژی‌ها

- **Next.js 16** - Framework React
- **TypeScript** - Type Safety
- **Tailwind CSS** - Styling
- **React Context** - State Management

## 📁 ساختار پروژه

```
frontend/
├── app/                    # صفحات Next.js (App Router)
│   ├── page.tsx           # صفحه اصلی
│   ├── login/             # صفحه ورود
│   ├── register/          # صفحه ثبت‌نام
│   ├── products/          # صفحات محصولات
│   └── dashboard/         # داشبورد کاربری
├── components/            # کامپوننت‌های React
│   ├── ui/               # کامپوننت‌های UI پایه
│   └── Navbar.tsx        # نوار ناوبری
├── contexts/              # React Contexts
│   └── AuthContext.tsx   # مدیریت احراز هویت
├── lib/                   # کتابخانه‌ها و utilities
│   └── api.ts            # API Client
└── types/                 # TypeScript types
```

## 🔧 تنظیمات

### متغیرهای محیطی

فایل `.env`:

```env
# API URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# Landing Page URL (برای لینک "High Win Rate" در Navbar)
NEXT_PUBLIC_LANDING_URL=http://localhost:3003

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Next.js Configuration
NEXT_PUBLIC_APP_NAME=High Win Rate
```

**⚠️ برای Production (Vercel/Deployment):**
در Vercel یا platform deploy، این environment variables را تنظیم کنید:

```env
NEXT_PUBLIC_API_URL=https://api.highwinrate.com
NEXT_PUBLIC_LANDING_URL=https://highwinrate.com
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### پورت پیش‌فرض

فرانت‌اند روی پورت **3001** اجرا می‌شود (بک‌اند روی پورت 3000).

## 📝 اسکریپت‌ها

- `npm run dev` - راه‌اندازی سرور توسعه
- `npm run build` - ساخت برای production
- `npm run start` - اجرای نسخه production
- `npm run lint` - بررسی کد با ESLint

## 🔗 اتصال به Backend

فرانت‌اند به صورت پیش‌فرض به بک‌اند در `http://localhost:3000` متصل می‌شود.

**مهم**: قبل از راه‌اندازی فرانت‌اند، مطمئن شوید که بک‌اند در حال اجرا است.

## 🐛 عیب‌یابی

### مشکل: خطای اتصال به API

**راه‌حل**:
1. بررسی کنید که بک‌اند در حال اجرا است
2. بررسی کنید که `NEXT_PUBLIC_API_URL` در `.env` درست تنظیم شده است
3. بررسی کنید که CORS در بک‌اند فعال است

### مشکل: خطای 401 Unauthorized

**راه‌حل**:
1. بررسی کنید که توکن JWT در localStorage ذخیره شده است
2. دوباره وارد شوید

### مشکل: خطای TypeScript

**راه‌حل**:
```bash
npm install
npm run build
```

## 📚 مستندات بیشتر

برای اطلاعات بیشتر درباره API و بک‌اند، به [مستندات پروژه](../docs/README.md) مراجعه کنید.

## 🚀 استقرار

برای استقرار در production:

```bash
npm run build
npm run start
```

یا از Docker استفاده کنید:

```bash
docker-compose -f docker-compose.dev.yml up
```

---

**نکته**: این فرانت‌اند برای استفاده با بک‌اند High Win Rate طراحی شده است.
